<hw-card>

    <impure-card if={opts.source._class=="IMPURE"}
                 source={opts.source}
                 maledict={opts.maledict}
                 open={opts.open}
                 callbacks={opts.callbacks}></impure-card>

</hw-card>
