<page-impure-card-deamon>

    <page-impure-card-deamon-large if={opts.open}
                                   source={opts.source}
                                   callback={opts.callback}></page-impure-card-deamon-large>

    <page-impure-card-deamon-small if={!opts.open}
                                   source={opts.source}
                                   callback={opts.callback}></page-impure-card-deamon-small>

</page-impure-card-deamon>
